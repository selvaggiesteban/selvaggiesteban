name: Deploy to Cloudflare Workers

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Deploy
        id: deploy
        run: |
          set +e
          node node_modules/wrangler/bin/wrangler.js deploy --config dist/server/wrangler.json > /tmp/deploy-out.txt 2>&1
          EXIT_CODE=$?
          cat /tmp/deploy-out.txt
          echo "Deploy exit code: $EXIT_CODE"
          exit $EXIT_CODE
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
